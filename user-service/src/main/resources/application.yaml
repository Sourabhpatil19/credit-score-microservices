server:
     port: 8080
spring:
       application:
         name: user-service
       datasource:
         url: jdbc:mysql://localhost:3306/credit_system
         username: root
         password: root
       jpa:
          hibernate:
           ddl-auto: update
          show-sql: true
